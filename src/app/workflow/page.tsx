
import WorkflowContainer from "@/Components/Workflow/containers/workflow-container";

export default async function ChatPage() {
    return (
        <main className="min-h-screen bg-[var(--bg-dark)] text-white">
            <WorkflowContainer />
        </main>
    );
}